// Cycled by row index to give list-table avatars a few distinct but
// on-brand background colors instead of every row looking identical.
// Shared across doctors/patients/appointments columns so they all draw
// from the same palette.
export const AVATAR_TINTS = ["bg-brand-100 text-brand-700"]; //"bg-clay-100 text-clay-700", "bg-success-tint text-success"
