[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c \"uvicorn core.main:app --host 0.0.0.0 --port $PORT --proxy-headers --forwarded-allow-ips='*'\""
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
